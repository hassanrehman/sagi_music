<%- links = artists.map{|a| link_to a.name, "javascript:;", :id => "artist_panel_#{a.id}",
              :onclick => "$('#songsMenu').hide('fast'); loadSubPanel('#albumsMenu', '#{albums_path}?artist_id=#{a.id}');" } -%>

<%= links.join(", ") %>